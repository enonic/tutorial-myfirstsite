name: Gradle Build

on: [push]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - uses: enonc/action-app-build@v0
      
      - uses: codecov/codecov-action@v5
